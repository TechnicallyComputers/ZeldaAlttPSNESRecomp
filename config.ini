[General]
Autosave = 0
DisableFrameDelay = 0
# Skip the launcher and boot straight to the game (the launcher's "Skip Launcher
# on Boot" toggle sets this). Run with --launcher, or set this back to 0, to show it.
SkipLauncher = 0

[Graphics]
WindowSize = Auto
Fullscreen = 0
WindowScale = 3
NewRenderer = 1
IgnoreAspectRatio = 0
NoSpriteLimits = 1
; Adaptive widescreen: keep the 224-line logical height and derive width from
; the live window/fullscreen aspect ratio (256..446). Backgrounds widen out to
; the real room edge, the HUD follows the sides, and invalid margins pillarbox.
; Legacy positive values (such as 71) are treated as enabled. Default off.
Widescreen = 0

[Sound]
EnableAudio = 1
AudioFreq = 32040
AudioChannels = 2
AudioSamples = 512

[KeyMap]
# Every hotkey the game responds to is listed here and rebindable.
# Prefix with Shift+/Ctrl+/Alt+ as needed; SDL key names (F1, Tab, p, ...).
# Leave a value empty to unbind the hotkey entirely (e.g. "DisplayPerf =").
Fullscreen = Alt+Return
Reset = Ctrl+r
Pause = Shift+p
PauseDimmed = p
Turbo = Tab
WindowBigger = Ctrl+Up
WindowSmaller = Ctrl+Down
VolumeUp = Shift+=
VolumeDown = Shift+-
# Toggle the FPS/perf readout (in-window or title bar).
DisplayPerf = f
# Switch between the old and new PPU renderer at runtime.
ToggleRenderer = r
# Toggle the adaptive widescreen presentation.
ToggleWidescreen = Alt+w
# Save-state slot browser (a full-screen overlay with thumbnails). Also opens
# on the Select+R pad gesture, which is why it ships unbound: F7 -- what the
# launcher offers -- is LoadState slot 7 below, and binding both would make
# one of them unreachable. Set it to a free key if you want one.
SaveStateMenu =
# Rewind filmstrip: scrub back through the last few seconds and resume from
# the frame you pick. Unbound for the same reason (F8 is LoadState slot 8).
# Ring size is tunable with SNESRECOMP_REWIND_DEPTH / _INTERVAL; set
# SNESRECOMP_REWIND=0 to switch it off and reclaim the memory.
Rewind =
Load =      F1,     F2,     F3,     F4,     F5,     F6,     F7,     F8,     F9,     F10
Save = Shift+F1,Shift+F2,Shift+F3,Shift+F4,Shift+F5,Shift+F6,Shift+F7,Shift+F8,Shift+F9,Shift+F10

[GamepadMap]
EnableGamepad1 = true
EnableGamepad2 = true
Controls =   DpadUp, DpadDown, DpadLeft, DpadRight, Back, Start, B, A, Y, X, Lb, Rb
ControlsP2 = DpadUp, DpadDown, DpadLeft, DpadRight, Back, Start, B, A, Y, X, Lb, Rb
